# Configuration

`laranail/console-tools` works out of the box with no configuration.

Formatting is configured per-instance rather than via a published config
file — e.g. `ConsoleUIFormatter::create()->configure([...])` accepts
padding/display option arrays.

---

[← Docs index](../README.md#documentation)
